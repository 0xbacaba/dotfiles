source = config/default.conf

# generated by nwg-displays (https://github.com/nwg-piotr/nwg-displays)
source = monitors.conf
source = workspaces.conf

# apps.conf needs to be sourced before binds
source = config/apps.conf

source = config/binds.conf

source = config/rules.conf
